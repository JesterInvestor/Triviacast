'use client';

// Deprecated: ThirdwebProvider retained as a stub for backward compatibility.
// All wallet functionality has migrated to wagmi + WalletConnect.
// Safe to remove once no imports reference this component.

export default function ThirdwebProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>; // no-op wrapper
}
